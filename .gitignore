# ----- Python -----
.venv/
__pycache__/
*.py[cod]
*.pyc

# ----- PyInstaller build artifacts -----
build/
dist/
*.spec

# ----- Editors / OS -----
.idea/
.vscode/
*.swp
Thumbs.db
Desktop.ini

# ----- Runtime user config -----
# User config / favorites / cache files live in %APPDATA%/wallery (Windows)
# or the platform config dir and must NOT be committed.
config.json
favorites.json
wallpaper_cache.json

# ----- Local secrets (NEVER commit) -----
# KEYS.md / API-KEYS.md hold real tokens; they live outside this repo too.
KEYS.md
API-KEYS.md
*.local
.env
